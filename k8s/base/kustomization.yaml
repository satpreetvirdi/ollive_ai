apiVersion: kustomize.config.k8s.io/v1beta1
kind: Kustomization

namespace: olliveai

resources:
  - namespace.yaml
  - configmap.yaml
  - postgres-configmap.yaml
  - postgres.yaml
  - redis.yaml
  - ingestion-api.yaml
  - ingestion-worker.yaml
  - chat-api.yaml
  - web.yaml

# secret.yaml is NOT included — create from secret.example.yaml before apply
